保持`(venv)`激活状态，执行：

bash

```
pip install requests python-dotenv langchain langchain-community chromadb pydantic
```

包说明：

- requests：http 请求，调用 LLM API
- python-dotenv：读取.env 文件，存放密钥（避免硬编码 API Key）
- langchain、langchain-community：Agent 框架（后面学习用）
- chromadb：本地向量库，RAG 长期记忆
- pydantic：数据校验，工具参数解析