[build]
  command = "npm ci && npx quartz plugin install && npx quartz build"
  publish = "public"

[build.environment]
  NODE_VERSION = "24"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200